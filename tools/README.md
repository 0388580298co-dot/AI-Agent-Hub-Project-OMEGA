# Tool Ecosystem

Tool implementations are grouped by capability. The secure runtime and baseline adapters live in `src/omega/tools/`.

Every capability should expose a typed contract, explicit schema, authorization boundary, timeout and normalized failure behavior.