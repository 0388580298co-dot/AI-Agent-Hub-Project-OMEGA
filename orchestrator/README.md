# Orchestration Layer

The orchestration domain owns agent runs, graph scheduling, state transitions, cancellation, budgets and future durable checkpoints.

Canonical Python runtime: `src/omega/orchestrator/`.