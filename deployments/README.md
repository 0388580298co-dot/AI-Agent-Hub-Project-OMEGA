# Deployments

Deployment manifests belong here as OMEGA grows into a service-oriented runtime.

Future targets include local Docker development and Kubernetes worker/control-plane deployments. No deployment artifact is advertised until it is runnable and tested.