# Agent Layer

Application-level agent definitions and reusable role templates. The canonical runtime implementation lives under `src/omega/agents/`.

Planned role packages include planning, research, coding, review, reflection and policy-aware agent templates.