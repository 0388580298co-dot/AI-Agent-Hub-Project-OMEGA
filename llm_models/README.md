# LLM Model Layer

Model-provider integration concepts and deployment configuration. Runtime contracts are implemented in `src/omega/llm_models/`.

The gateway isolates provider-specific transport, routing and token policy from agent cognition.