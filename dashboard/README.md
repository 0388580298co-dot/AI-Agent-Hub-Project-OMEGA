# OMEGA Operations Dashboard

The future control plane for inspecting agent runs, graph state, tool calls, memory retrieval, model usage and evaluation metrics.

The current repository keeps the runtime framework-first; the React/TypeScript UI will consume the future API and realtime event stream.